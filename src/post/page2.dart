part of '../main.dart';

class Page2 extends StatelessWidget {
  const Page2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        color: const Color.fromARGB(255, 195, 222, 230),
        child: const Center(
          child: Text("Page2"),
        ));
  }
}
